
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
.expo/
# @end expo-cli

node_modules/
.env
.env.local
.env.*.local
dist/
dist-android/
dist-share/
dist-demo-check/
android/.gradle/
android/build/
android/app/build/
*.log
*.png
# App icon assets MUST be versioned (override the blanket *.png ignore above),
# otherwise app.json `icon` + the iOS AppIcon.appiconset build with no icon.
!assets/icon.png
!ios/SwipeClean/Images.xcassets/AppIcon.appiconset/App-Icon-1024x1024@1x.png
.codex-remote-attachments/

# Local tooling / run artifacts (Maestro CLI binary, logs, test media, helper
# scripts) — all machine-local. The committed Maestro FLOWS live in .maestro/
# and are intentionally tracked.
.codex/
.maestro/screenshots/

# Metro bundle warmup artifact (machine-local)
bundle-warm.out

# Agent/scratch output tree (local research + creative artifacts, can be large)
outputs/

# Dev-server QR codes — they encode this machine's Metro LAN/tunnel URL
qr-*.svg

# Native module Gradle/CMake build outputs
modules/*/android/build/
modules/*/android/.cxx/

# Release signing — NEVER commit the production keystore or its passwords.
# (The template debug.keystore is intentionally tracked; these are the real ones.)
android/keystore.properties
android/release.jks
*.jks

# Built app bundles / signed release copies — never commit artifacts
*.aab
*.apk

# macOS Finder junk
.DS_Store

# iOS native project — track sources, ignore generated/build artifacts
ios/Pods/
ios/build/
ios/.xcode.env.local
ios/*.xcworkspace/xcuserdata/
ios/*.xcodeproj/xcuserdata/
